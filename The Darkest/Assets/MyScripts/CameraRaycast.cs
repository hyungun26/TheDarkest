using System.Collections;
using System.Collections.Generic;
using System.Linq;
using UnityEngine;

public class CameraRaycast : MonoBehaviour
{
    //public GameObject arrow;
    //// Update is called once per frame
    //void Update()
    //{
    //    int layerMask = 1 << LayerMask.NameToLayer("Monster");

    //    Debug.DrawRay(this.transform.position, this.transform.forward * 10.0f);
    //    if (Physics.Raycast(this.transform.position,this.transform.forward, out RaycastHit hit, 10.0f, layerMask))
    //    {
    //        Debug.Log(hit.collider.gameObject);
    //        if(Input.GetMouseButtonDown(0))
    //        {
    //            Instantiate(arrow, this.transform.position, this.transform.rotation);
    //            Debug.Log("화살생성");
    //        }
    //    }    
    //}
}
